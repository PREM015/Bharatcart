import { FC } from 'react';

interface indexProps {
  // Add props here
}

const index: FC<indexProps> = (props) => {
  return (
    <div>
      <h2>index</h2>
    </div>
  );
};

export default index;
