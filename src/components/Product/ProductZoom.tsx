import { FC } from 'react';

interface ProductZoomProps {
  // Add props here
}

const ProductZoom: FC<ProductZoomProps> = (props) => {
  return (
    <div>
      <h2>ProductZoom</h2>
    </div>
  );
};

export default ProductZoom;
