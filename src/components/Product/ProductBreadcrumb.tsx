import { FC } from 'react';

interface ProductBreadcrumbProps {
  // Add props here
}

const ProductBreadcrumb: FC<ProductBreadcrumbProps> = (props) => {
  return (
    <div>
      <h2>ProductBreadcrumb</h2>
    </div>
  );
};

export default ProductBreadcrumb;
