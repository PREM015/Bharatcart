import { FC } from 'react';

interface ProductSpecsProps {
  // Add props here
}

const ProductSpecs: FC<ProductSpecsProps> = (props) => {
  return (
    <div>
      <h2>ProductSpecs</h2>
    </div>
  );
};

export default ProductSpecs;
