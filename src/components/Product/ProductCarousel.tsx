import { FC } from 'react';

interface ProductCarouselProps {
  // Add props here
}

const ProductCarousel: FC<ProductCarouselProps> = (props) => {
  return (
    <div>
      <h2>ProductCarousel</h2>
    </div>
  );
};

export default ProductCarousel;
