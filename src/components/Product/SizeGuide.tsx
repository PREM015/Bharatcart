import { FC } from 'react';

interface SizeGuideProps {
  // Add props here
}

const SizeGuide: FC<SizeGuideProps> = (props) => {
  return (
    <div>
      <h2>SizeGuide</h2>
    </div>
  );
};

export default SizeGuide;
