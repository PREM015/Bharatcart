import { FC } from 'react';

interface VariantSelectorProps {
  // Add props here
}

const VariantSelector: FC<VariantSelectorProps> = (props) => {
  return (
    <div>
      <h2>VariantSelector</h2>
    </div>
  );
};

export default VariantSelector;
