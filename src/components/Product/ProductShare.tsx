import { FC } from 'react';

interface ProductShareProps {
  // Add props here
}

const ProductShare: FC<ProductShareProps> = (props) => {
  return (
    <div>
      <h2>ProductShare</h2>
    </div>
  );
};

export default ProductShare;
