import { FC } from 'react';

interface ProductTabsProps {
  // Add props here
}

const ProductTabs: FC<ProductTabsProps> = (props) => {
  return (
    <div>
      <h2>ProductTabs</h2>
    </div>
  );
};

export default ProductTabs;
