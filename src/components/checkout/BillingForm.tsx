import { FC } from 'react';

interface BillingFormProps {
  // Add props here
}

const BillingForm: FC<BillingFormProps> = (props) => {
  return (
    <div>
      <h2>BillingForm</h2>
    </div>
  );
};

export default BillingForm;
