import { FC } from 'react';

interface GuestCheckoutProps {
  // Add props here
}

const GuestCheckout: FC<GuestCheckoutProps> = (props) => {
  return (
    <div>
      <h2>GuestCheckout</h2>
    </div>
  );
};

export default GuestCheckout;
