import { FC } from 'react';

interface AddressSelectorProps {
  // Add props here
}

const AddressSelector: FC<AddressSelectorProps> = (props) => {
  return (
    <div>
      <h2>AddressSelector</h2>
    </div>
  );
};

export default AddressSelector;
