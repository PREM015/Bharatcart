import { FC } from 'react';

interface OrderNotesProps {
  // Add props here
}

const OrderNotes: FC<OrderNotesProps> = (props) => {
  return (
    <div>
      <h2>OrderNotes</h2>
    </div>
  );
};

export default OrderNotes;
