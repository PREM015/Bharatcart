import { FC } from 'react';

interface ExpressCheckoutProps {
  // Add props here
}

const ExpressCheckout: FC<ExpressCheckoutProps> = (props) => {
  return (
    <div>
      <h2>ExpressCheckout</h2>
    </div>
  );
};

export default ExpressCheckout;
