import { FC } from 'react';

interface GiftOptionsProps {
  // Add props here
}

const GiftOptions: FC<GiftOptionsProps> = (props) => {
  return (
    <div>
      <h2>GiftOptions</h2>
    </div>
  );
};

export default GiftOptions;
