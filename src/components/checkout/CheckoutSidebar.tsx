import { FC } from 'react';

interface CheckoutSidebarProps {
  // Add props here
}

const CheckoutSidebar: FC<CheckoutSidebarProps> = (props) => {
  return (
    <div>
      <h2>CheckoutSidebar</h2>
    </div>
  );
};

export default CheckoutSidebar;
