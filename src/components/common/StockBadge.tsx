import { FC } from 'react';

interface StockBadgeProps {
  // Add props here
}

const StockBadge: FC<StockBadgeProps> = (props) => {
  return (
    <div>
      <h2>StockBadge</h2>
    </div>
  );
};

export default StockBadge;
