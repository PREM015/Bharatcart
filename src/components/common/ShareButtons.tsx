import { FC } from 'react';

interface ShareButtonsProps {
  // Add props here
}

const ShareButtons: FC<ShareButtonsProps> = (props) => {
  return (
    <div>
      <h2>ShareButtons</h2>
    </div>
  );
};

export default ShareButtons;
