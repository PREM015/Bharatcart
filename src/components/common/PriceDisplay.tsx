import { FC } from 'react';

interface PriceDisplayProps {
  // Add props here
}

const PriceDisplay: FC<PriceDisplayProps> = (props) => {
  return (
    <div>
      <h2>PriceDisplay</h2>
    </div>
  );
};

export default PriceDisplay;
