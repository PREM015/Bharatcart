import { FC } from 'react';

interface RatingProps {
  // Add props here
}

const Rating: FC<RatingProps> = (props) => {
  return (
    <div>
      <h2>Rating</h2>
    </div>
  );
};

export default Rating;
