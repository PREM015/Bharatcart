import { FC } from 'react';

interface DiscountBadgeProps {
  // Add props here
}

const DiscountBadge: FC<DiscountBadgeProps> = (props) => {
  return (
    <div>
      <h2>DiscountBadge</h2>
    </div>
  );
};

export default DiscountBadge;
