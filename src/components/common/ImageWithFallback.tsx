import { FC } from 'react';

interface ImageWithFallbackProps {
  // Add props here
}

const ImageWithFallback: FC<ImageWithFallbackProps> = (props) => {
  return (
    <div>
      <h2>ImageWithFallback</h2>
    </div>
  );
};

export default ImageWithFallback;
