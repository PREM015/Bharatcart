import { FC } from 'react';

interface CookieConsentProps {
  // Add props here
}

const CookieConsent: FC<CookieConsentProps> = (props) => {
  return (
    <div>
      <h2>CookieConsent</h2>
    </div>
  );
};

export default CookieConsent;
