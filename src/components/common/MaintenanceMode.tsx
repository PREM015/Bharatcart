import { FC } from 'react';

interface MaintenanceModeProps {
  // Add props here
}

const MaintenanceMode: FC<MaintenanceModeProps> = (props) => {
  return (
    <div>
      <h2>MaintenanceMode</h2>
    </div>
  );
};

export default MaintenanceMode;
