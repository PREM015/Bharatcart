import { FC } from 'react';

interface TrackingInfoProps {
  // Add props here
}

const TrackingInfo: FC<TrackingInfoProps> = (props) => {
  return (
    <div>
      <h2>TrackingInfo</h2>
    </div>
  );
};

export default TrackingInfo;
