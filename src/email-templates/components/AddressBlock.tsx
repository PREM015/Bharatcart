import { FC } from 'react';

interface AddressBlockProps {
  // Add props here
}

const AddressBlock: FC<AddressBlockProps> = (props) => {
  return (
    <div>
      <h2>AddressBlock</h2>
    </div>
  );
};

export default AddressBlock;
