import { FC } from 'react';

interface PriceBreakdownProps {
  // Add props here
}

const PriceBreakdown: FC<PriceBreakdownProps> = (props) => {
  return (
    <div>
      <h2>PriceBreakdown</h2>
    </div>
  );
};

export default PriceBreakdown;
