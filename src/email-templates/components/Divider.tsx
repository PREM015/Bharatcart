import { FC } from 'react';

interface DividerProps {
  // Add props here
}

const Divider: FC<DividerProps> = (props) => {
  return (
    <div>
      <h2>Divider</h2>
    </div>
  );
};

export default Divider;
