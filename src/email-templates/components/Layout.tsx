import { FC } from 'react';

interface LayoutProps {
  // Add props here
}

const Layout: FC<LayoutProps> = (props) => {
  return (
    <div>
      <h2>Layout</h2>
    </div>
  );
};

export default Layout;
