import { FC } from 'react';

interface optionsProps {
  // Add props here
}

const options: FC<optionsProps> = (props) => {
  return (
    <div>
      <h2>options</h2>
    </div>
  );
};

export default options;
