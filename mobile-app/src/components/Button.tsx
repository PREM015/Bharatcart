import { FC } from 'react';

interface ButtonProps {
  // Add props here
}

const Button: FC<ButtonProps> = (props) => {
  return (
    <div>
      <h2>Button</h2>
    </div>
  );
};

export default Button;
