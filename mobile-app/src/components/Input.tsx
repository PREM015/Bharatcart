import { FC } from 'react';

interface InputProps {
  // Add props here
}

const Input: FC<InputProps> = (props) => {
  return (
    <div>
      <h2>Input</h2>
    </div>
  );
};

export default Input;
